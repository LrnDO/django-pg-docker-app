name: Build, Push and Deploy

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.vars.outputs.version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Define image name and version
        id: vars
        run: |
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            VERSION=${GITHUB_REF_NAME}
          else
            VERSION="latest"
          fi
          echo "Detected VERSION=$VERSION"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "IMAGE=${{ secrets.DOCKERHUB_USERNAME }}/django-pg-tutorial:$VERSION"
        shell: bash

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/django-pg-tutorial:${{ steps.vars.outputs.version }}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Show version (debug)
        run: echo "Deploying version ${{ needs.build.outputs.version }}"

      - name: Deploy via SSH to Kubernetes master
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            export KUBECONFIG=$HOME/.kube/config
            cd /home/ubuntu/django-pg-docker-tutorial || exit 1
            git pull origin main
            echo "Deploying image: ${{ secrets.DOCKERHUB_USERNAME }}/django-pg-tutorial:${{ needs.build.outputs.version }}"
            helm upgrade --install django-tutorial ./DTchart \
              --set app.image=${{ secrets.DOCKERHUB_USERNAME }}/django-pg-tutorial:${{ needs.build.outputs.version }} \
              --namespace default

